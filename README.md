# scalalang2

Hugo blog using the `hugo-bearneo` theme.

## Commands

```powershell
node scripts\migrate-medium.mjs
node scripts\check-content.mjs
hugo server -D
hugo
```

The Medium export is kept in `backup/`. Migrated posts live in `content/blog/`.
